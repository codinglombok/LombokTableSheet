{
    "name": "codinglombok/lomboktablesheet",
    "description": "Fast, lightweight, portable Table + Spreadsheet library — PHP port of LombokTableSheet.",
    "type": "library",
    "license": "Apache-2.0",
    "authors": [
        { "name": "codinglombok" }
    ],
    "require": {
        "php": ">=8.1"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.6"
    },
    "autoload": {
        "classmap": ["src/"]
    },
    "autoload-dev": {
        "classmap": ["tests/"]
    },
    "scripts": {
        "test": "phpunit --testdox tests"
    }
}
