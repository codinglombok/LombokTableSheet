[package]
name = "lomboktablesheet"
version = "0.1.1"
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/codinglombok/LombokTableSheet"
description = "Rust port — incomplete, only formula.rs exists so far"
publish = false

# Only src/formula.rs exists today. The lib, test, and example entries that
# used to be here pointed at files that were never created, which made
# `cargo build` fail and caused CodeQL to report "Low Rust analysis quality".
# They will be restored as the files are actually written.
